<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.godman.anvil.dao.AnvilUserDao">

	<select id="findByUsername" parameterType="Map" resultType="AnvilUser">
		SELECT
		u..username,
		u.password,
		u.last_password_reset_date,
		 r.name
		FROM anvil_user u, anvil_role r
		WHERE u.username=#{username} and r.id=u.role_id
	</select>

</mapper>